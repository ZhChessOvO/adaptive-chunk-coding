# A800 单卡一次性独立测试协议

## 授权与目的

用户在 2026-09-18 明确授权读取此前未使用的 REDS `val/012..023`。这 12 条视频只用于
一次性独立测试：验证已经冻结的控制器、低／中预算点和常驻 ROI 执行能否从开发集
`val/000..005` 泛化。看到测试结果后不再用这 12 条视频调参数、改 checkpoint 或重新
选择预算点。

`val/024..029` 继续封存，不能读取。DCVC-UF、SeedVR2 和 5768 参数控制器都保持冻结；
不生成新 teacher 标签，不重新训练，也不进入多卡或模型／codec 微调。

正式输出固定写到：

```text
/root/autodl-fs/DCVC/runs/a800_independent_test_20260918
```

## 在读取图片前固定的样本

- 测试序列：`012..023`，每条恰好取一个样本，共 12 个；
- 每条使用第 0–16 帧，共 17 帧；
- 使用与开发集相同的 512×512 中心对齐裁剪：`x=384, y=96`；
- 固定第 9 帧做可视化；
- 按序列号从小到大运行；随机种子固定为 `20260917 + 2000000 + 序列号`；
- 不根据视频内容筛样本、换裁剪、换帧窗或排除困难样本。

## 冻结的方法和对照

控制器 checkpoint 固定为第一轮单卡试跑保存的
`a800_pilot_20260917/pilot/controller/controller.pt`。求解器参数保持不变：Enhance
fallback 字节比例为 `0.25 / 0.50 / 1.00`，Generate tile 上限为 `4 / 8 / 12`。

本次只正式检验已经在开发阶段声明的两个点：

- 低预算：`mlp-budget-0`；
- 中预算：`mlp-budget-1`。

每条测试视频固定报告：

1. 均匀 QP8／16／24／32；
2. 相同四个 QP 的 BasicVSR++ 确定性对照；
3. 全 Generate 和 Enhance-only；
4. 低预算联合路由、同路由关闭 Generate、同路由关闭 Enhance；
5. 中预算联合路由、同路由关闭 Generate、同路由关闭 Enhance。

所有空间流按真实落盘字节计费，包含头部、action map 和全部子流。每条流必须在独立
进程 fresh decode，且与编码端重建逐像素一致。Generate 路径采用常驻 ROI：同一个
样本、同一个路由只加载一次 SeedVR2。正式时间包含 fresh decode、模型进程加载、ROI
恢复和拼接；正式运行前只用已消费的开发样本做一次不计入结果的文件缓存预热。

## 预先固定的报告和判断

LPIPS 是主要质量指标；同时报告 PSNR、时序误差、真实字节、完整解码时间和峰值显存。
低／中预算分别与每条视频字节最接近的**已测离散均匀 QP 点**比较，不把它写成严格
同码率插值。

对低预算和中预算分别报告以下数字：

- 联合路由相对最近均匀 QP 的逐样本 LPIPS 差值、12 条均值和改善条数；
- 联合路由相对全 Generate 的质量、字节和时间，但明确这不是同码率比较；
- 关闭 Generate／Enhance 后的 LPIPS 差值和变差条数；
- Base／Generate／Enhance 动作总数；
- 完整时间是否低于全 Generate。

预先固定的“独立测试支持开发结论”标准为：

1. 联合路由相对最近均匀 QP 的平均 LPIPS 更低，并且至少 8/12 条更低；
2. 关闭 Generate 和关闭 Enhance 的平均 LPIPS 都变差，并且各至少 8/12 条变差；
3. 联合路由的平均完整时间低于全 Generate，并且至少 8/12 条更快；
4. 所有真实流的 fresh decode 均逐像素一致。

低预算和中预算各自独立判定。全 Generate 比较、PSNR、时序和动作比例无论正负都完整
报告，不作为事后改规则的理由。若任何判据失败，保留负结果并停止，不回到这 12 条
视频继续调参。

## 工程边界

- 长任务在 tmux 中运行，每分钟记录进度、GPU 和三块盘；
- 样本和阶段使用原子完成标志，可在服务器重启后从完整样本继续；
- 中断在 ROI batch 内时整批小任务重跑，不能只补剩余组件并低估时间；
- 文件存储或数据盘达到 80% 时停止新样本；
- 测试数据、码流、PNG、日志和正式结果不进入 Git，也不记录文件哈希；
- 结果完成后更新 README、云端文档和 Notion，再单独提交结果代码与文档。

## 2026-09-18 实际执行结果

本轮已在同一张 A800 80GB 上完成，12/12 个样本均有原子完成标志，48/48 个常驻
ROI batch 元数据齐全。没有生成新 teacher、训练控制器、调整预算或改变任何模型
权重。测试图片来自 REDS 作者维护的
[数据集页面](https://seungjunnah.github.io/Datasets/reds.html)和
[Hugging Face 仓库](https://huggingface.co/datasets/snah/REDS)；只解压了已授权的
`012..023`，归档随后删除，`024..029` 仍未解压或读取。

下面是 17 帧样本的 12 条视频均值。字节均来自真实落盘码流，完整时间包含 fresh
decode、模型进程加载、ROI 恢复和拼接。

| 方法 | 平均落盘字节 | bpp | LPIPS↓ | PSNR dB↑ | 时序 MAE↓ | 完整时间（秒） | 峰值显存 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 均匀 QP8 | 5239.2 | 0.009405 | 0.615235 | 21.890 | 18.471 | 6.527 | 0.38 GiB |
| 均匀 QP16 | 9199.3 | 0.016514 | 0.545558 | 22.965 | 16.778 | 6.562 | 0.38 GiB |
| 均匀 QP24 | 16162.8 | 0.029015 | 0.475820 | 24.063 | 15.160 | 6.522 | 0.38 GiB |
| 均匀 QP32 | 28513.2 | 0.051186 | 0.403449 | 25.223 | 13.536 | 6.459 | 0.38 GiB |
| 全 Generate | 5382.6 | 0.009663 | 0.510759 | 20.689 | 19.588 | 29.411 | 14.96 GiB |
| Enhance-only | 28645.2 | 0.051423 | 0.404198 | 25.217 | 13.544 | 6.443 | 0.34 GiB |
| 低预算联合 G/B/E | 12931.3 | 0.023214 | 0.488090 | 22.768 | 16.509 | 20.715 | 17.65 GiB |
| 低预算关闭 Generate | 14086.5 | 0.025287 | 0.497198 | 23.405 | 15.898 | 6.536 | 0.34 GiB |
| 低预算关闭 Enhance | 8248.7 | 0.014808 | 0.535816 | 22.390 | 17.395 | 20.655 | 17.65 GiB |
| 中预算联合 G/B/E | 17193.4 | 0.030865 | 0.451470 | 22.971 | 16.061 | 21.602 | 18.33 GiB |
| 中预算关闭 Generate | 18914.1 | 0.033954 | 0.460296 | 23.908 | 15.113 | 6.422 | 0.34 GiB |
| 中预算关闭 Enhance | 7719.8 | 0.013858 | 0.533157 | 22.168 | 17.763 | 21.425 | 18.33 GiB |

完整的四档 BasicVSR++ 数字保存在正式汇总中；它在四档 QP 上均没有降低平均 LPIPS，
因此不能作为本轮主结论的来源。

### 低预算：平均值改善，但没有通过预注册稳定性门槛

- 192 个区域共选择 Base 77、Generate 41、Enhance 74；
- 相对每条视频字节最近的已测均匀 QP，LPIPS 平均降低 0.007339，但只有 5/12 条
  更好，未达到预先写下的至少 8/12；
- 关闭 Generate 后平均变差 0.009108，11/12 条变差；关闭 Enhance 后平均变差
  0.047726，12/12 条变差，两条分支贡献判据都通过；
- 平均完整时间比全 Generate 快 29.57%，12/12 条更快，速度判据通过；
- 因质量一致性判据失败，低预算的总判定必须记为**不支持开发结论**，不能用平均值
  略好掩盖逐样本不稳定。

### 中预算：四项预注册判据全部通过

- 192 个区域共选择 Base 15、Generate 57、Enhance 120；
- 相对每条视频字节最近的已测均匀 QP，LPIPS 平均降低 0.024350，12/12 条更好；
- 关闭 Generate 后平均变差 0.008826，10/12 条变差；关闭 Enhance 后平均变差
  0.081687，12/12 条变差；
- 平均完整时间 21.602 秒，比全 Generate 的 29.411 秒快 26.55%，12/12 条更快；
- 因此中预算的质量、两条分支贡献、速度和 fresh decode 四项判据全部通过，
  **支持开发阶段的中预算结论**。

全 Generate 比较不是同码率比较：低／中预算 LPIPS 分别平均低 0.022669 和 0.059290，
但也使用更多码流字节。这个数字只说明实际工作点差异，不能替代最近离散 QP 对照。

### 完整性、可视化和资源

- 全部 scalar 与空间码流 fresh decode 都与编码端重建逐像素一致；真实文件大小与记录
  字节全部一致；
- 每个样本固定保存第 9 帧的 17 面板。已人工检查首、中、末样本，无空图、错位或缺
  面板，动作图和对应输出位置一致；
- 总墙钟 6419 秒，约 1 小时 47 分；八种空间方案累计完整流水线时间 1598.51 秒；
- 正式峰值 CUDA allocated memory 为 19,685,220,352 B，即 18.33 GiB；单张 A800
  余量充分，没有使用多卡；
- 正式目录普通文件合计 2,784,200,847 B，`du -sb` 为 2,789,296,271 B，共
  10617 个文件；结束时三块盘约为 11.10%／16.68%／23.28%；
- 权威结果为 `formal/independent_summary.json`（另有 `.md`、`.csv`），资源快照为
  `final_resource_snapshot.json`，实际脚本为
  `logs/executed_run_a800_independent_test.sh`；这些大文件都不进入 Git。

首次收尾时发现资源快照把仍在增长的日志和自身写入也纳入固定点，导致正式结果已经
完成但收尾检查失败。修复后改为精确统计不再变化的正式负载，并明确排除实时日志和
快照自身；断点续跑只重做收尾，12 条正式结果没有重算或改变。

## 结论与下一步

这轮给出了一个清晰的正负混合结论：**中预算已经在一次性独立测试上成立，低预算只在
平均值上略好，逐样本稳定性没有成立。** `val/012..023` 从现在起是已消费测试集，不能
再用于调参或宣称独立验证；`val/024..029` 继续封存。

下一步仍可只用这一张 A800。建议先在既有训练集和开发集上分析低预算为何只在 5/12 条
超过最近离散 QP，并改进低预算的跨视频稳定性；不能回看 `012..023` 选择修改。若要对
修改后的低预算作最终验证，应先写好新协议，再由用户明确授权一个仍未读取的数据集。
在此之前不要打开 `024..029`，也没有证据要求进入多卡或微调 SeedVR2／DCVC-UF。
