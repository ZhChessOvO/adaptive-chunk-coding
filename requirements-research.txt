# Install the CUDA-enabled torch/torchvision wheels first, as documented in
# README.md. This file records the tested auxiliary stack for the current
# Generate / Base / Enhance experiments.
-r requirements.txt

accelerate==1.14.0
diffusers==0.40.0
einops==0.7.0
huggingface-hub==1.29.0
lpips==0.1.4
mediapy==1.2.0
omegaconf==2.3.0
opencv-python-headless==4.10.0.84
rotary-embedding-torch==0.5.3
safetensors==0.8.0
transformers==5.16.1
